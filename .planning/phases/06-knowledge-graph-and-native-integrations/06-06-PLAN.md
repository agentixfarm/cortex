---
phase: 06-knowledge-graph-and-native-integrations
plan: 06
type: execute
wave: 4
depends_on: [06-03, 06-04, 06-05]
files_modified:
  - client/components/entities/EntityChip.tsx
  - client/components/entities/EntityTypeBadge.tsx
  - client/components/entities/EntityCard.tsx
  - client/components/entities/EntityTypeFilterBar.tsx
  - client/components/entities/EntityDetailHeader.tsx
  - client/components/entities/AliasChipList.tsx
  - client/components/entities/AliasChip.tsx
  - client/components/entities/RelatedEntityChip.tsx
  - client/components/entities/SplitAliasDialog.tsx
  - client/components/layout/BackfillIndicator.tsx
  - client/pages/EntitiesPage.tsx
  - client/pages/EntityDetailPage.tsx
  - client/hooks/useTauri.ts
  - client/hooks/useBackfillProgress.ts
  - client/lib/stores.ts
  - client/components/layout/Sidebar.tsx
  - client/components/layout/TopBar.tsx
  - client/components/layout/AppShell.tsx
  - client/App.tsx
  - client/lib/mock-data.ts
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [KG-01, KG-03, KG-04, KG-05]
user_setup: []
tags: [entity-ui, knowledge-graph, react-router, zustand, tauri-events, end-to-end-checkpoint]
must_haves:
  truths:
    - "/entities route renders an index of all canonical entities grouped by type with a 7-pill filter bar (All + 6 types)"
    - "/entities/:id route renders an EntityDetailHeader, AliasChipList, Documents-mentioning list, and Related-entities panel"
    - "Sidebar gains an Entities link under Tags using the Network lucide icon"
    - "TopBar gains a BackfillIndicator chip showing Brain icon + processed/total when status=running; flashes Done extracting entities for 3 seconds at complete; shows AlertCircle in red on error"
    - "useBackfillProgress listens to entity-backfill-progress Tauri events and routes them into useBackfillStore (Zustand)"
    - "useTauri.ts gains useEntities, useEntitiesByType, useEntity, useEntityDocuments, useRelatedEntities, useRenameEntityCanonical, useSplitEntityAlias"
    - "Clicking Split on an AliasChip opens SplitAliasDialog (shadcn AlertDialog); confirm invokes split_entity_alias IPC"
    - "Clicking the rename pencil on EntityDetailHeader enters inline edit mode; Enter saves via rename_entity_canonical IPC, Esc cancels"
    - "DocumentPage entity chips (Plan 05 inlined them as Links) now use the extracted EntityChip component — entityTypeIcon helper centralized"
    - ".planning/REQUIREMENTS.md is extended with a new Knowledge Graph section listing KG-01..KG-05 + UX-05 + PAGE-13 + UX-06 with Phase 6 traceability"
  artifacts:
    - path: "client/pages/EntitiesPage.tsx"
      provides: "Route /entities — type-grouped grid + filter bar"
      min_lines: 80
      contains: "useEntities"
    - path: "client/pages/EntityDetailPage.tsx"
      provides: "Route /entities/:id — header + aliases + docs + related"
      min_lines: 100
      contains: "useEntity"
    - path: "client/components/entities/EntityChip.tsx"
      provides: "Reusable entity chip (extracted from DocumentPage inline)"
      contains: "EntityChip"
    - path: "client/components/layout/BackfillIndicator.tsx"
      provides: "TopBar chip driven by useBackfillStore"
      contains: "BackfillIndicator"
    - path: "client/lib/stores.ts"
      provides: "useBackfillStore Zustand store"
      contains: "useBackfillStore"
    - path: "client/App.tsx"
      provides: "Routes /entities and /entities/:id registered inside AppShell"
      contains: "/entities"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Knowledge Graph requirements section + extended traceability"
      contains: "KG-01"
  key_links:
    - from: "client/components/layout/Sidebar.tsx"
      to: "/entities"
      via: "bottomLinks array entry"
      pattern: "label: \"Entities\""
    - from: "client/hooks/useBackfillProgress.ts"
      to: "entity-backfill-progress"
      via: "Tauri event listener writes to useBackfillStore"
      pattern: "entity-backfill-progress"
    - from: "client/pages/EntityDetailPage.tsx"
      to: "split_entity_alias"
      via: "useSplitEntityAlias mutation"
      pattern: "split_entity_alias"
    - from: "client/pages/EntityDetailPage.tsx"
      to: "rename_entity_canonical"
      via: "useRenameEntityCanonical mutation"
      pattern: "rename_entity_canonical"
---

<objective>
Final UI shipment of Phase 6: build the entity-discovery surfaces (/entities + /entities/:id), wire the React Query hooks for all 6 entity IPC commands from Plan 03, surface the NER backfill progress in the TopBar, and update REQUIREMENTS.md to declare the new KG / UX / PAGE requirements as v1 obligations.

Purpose:
- KG-01 (entities as graph nodes, click-through to documents) — EntityDetailPage Documents-mentioning section makes this user-visible
- KG-03 (knowledge graph queryable via IPC) — useTauri hooks make the IPC commands consumable from React
- KG-04 (Rename canonical + Split alias) — EntityDetailHeader inline edit + AliasChip Split → SplitAliasDialog → mutation
- KG-05 (NER backfill emits progress, UI stays responsive) — BackfillIndicator + useBackfillProgress event listener
- D-09 (entity chip → /entities/:id) — extracted EntityChip component finalizes the UX
- D-10 (entities sidebar link) — Sidebar bottomLinks gains Entities
- Doc cleanup — REQUIREMENTS.md updated to include the 8 new requirement IDs (per RESEARCH §Phase Requirements note: "KG-01..KG-05, UX-05, PAGE-13, UX-06 are NOT YET in REQUIREMENTS.md")

Output:
- 9 new entity components under client/components/entities/
- 2 new routes (EntitiesPage, EntityDetailPage)
- BackfillIndicator + TopBar integration + useBackfillProgress + useBackfillStore
- Entity React Query hooks in useTauri.ts (7 hooks: 5 queries + 2 mutations)
- DocumentPage swaps its inline chip render for the new EntityChip component
- Sidebar Entities link + App.tsx route registration
- REQUIREMENTS.md extension
- One blocking human-verify checkpoint at end of plan covering the full end-to-end UX (entity navigation, rename, split, backfill UI, native picker, preview)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-knowledge-graph-and-native-integrations/06-CONTEXT.md
@.planning/phases/06-knowledge-graph-and-native-integrations/06-RESEARCH.md
@.planning/phases/06-knowledge-graph-and-native-integrations/06-PATTERNS.md
@.planning/phases/06-knowledge-graph-and-native-integrations/06-UI-SPEC.md
@.planning/phases/06-knowledge-graph-and-native-integrations/06-03-SUMMARY.md
@.planning/phases/06-knowledge-graph-and-native-integrations/06-04-SUMMARY.md
@.planning/phases/06-knowledge-graph-and-native-integrations/06-05-SUMMARY.md
@CLAUDE.md
@client/App.tsx
@client/pages/SpacesPage.tsx
@client/pages/SpaceDetailPage.tsx
@client/pages/TagsPage.tsx
@client/pages/SearchPage.tsx
@client/pages/DocumentPage.tsx
@client/components/layout/Sidebar.tsx
@client/components/layout/TopBar.tsx
@client/components/layout/AppShell.tsx
@client/hooks/useTauri.ts
@client/lib/stores.ts
@client/lib/types.ts
@client/lib/mock-data.ts

<interfaces>
Key types/utilities (from prior plans + existing codebase):

From Plan 04 (client/lib/types.ts):
- CanonicalEntity, EntitySummary, RelatedEntity, EntityBackfillProgress all defined

From Plan 03 (Rust IPC commands — names exact for tauriInvoke):
- get_entities_by_type({ entityType?: string }) returns EntitySummary[]
- get_entity({ id }) returns CanonicalEntity
- get_documents_for_entity({ id }) returns Document[]
- get_related_entities({ id, minCoOccurrence?, limit? }) returns RelatedEntity[]
- rename_entity_canonical({ id, newName }) returns CanonicalEntity
- split_entity_alias({ canonicalId, alias }) returns CanonicalEntity (the NEW canonical)

From Plan 03 Tauri event:
- "entity-backfill-progress" payload shape EntityBackfillProgress { processed, total, status, error }

From existing client/hooks/useTauri.ts:
- queryKeys factory; pattern (useDocument lines 135-146) for query hooks; useToggleFavorite lines 224-234 for mutation hooks
- tauriInvoke(cmd, args, fallback?) generic helper

From existing client/lib/stores.ts:
- useIndexingStore at lines 44-79 — direct analog for useBackfillStore (Zustand with setProgress + reset)

From existing client/components/layout/TopBar.tsx (lines 37-56):
- Indexing-chip conditional render pattern — analog for BackfillIndicator placement

From existing client/components/layout/Sidebar.tsx (lines 54-59):
- bottomLinks array shape: { path, label, icon } — where to insert Entities entry

From existing client/pages/SpacesPage.tsx (lines 99-213):
- Direct analog for EntitiesPage skeleton + empty + populated states

From existing client/pages/SpaceDetailPage.tsx (lines 101-233):
- Direct analog for EntityDetailPage breadcrumb + sectioned-content layout

From shadcn primitives at client/components/ui/:
- alert-dialog (for SplitAliasDialog)
- tooltip (for chip hover tips)
- skeleton (for loading states)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Entity components + EntitiesPage + EntityDetailPage + routes + useTauri hooks + Sidebar link</name>
  <files>client/components/entities/EntityChip.tsx, client/components/entities/EntityTypeBadge.tsx, client/components/entities/EntityCard.tsx, client/components/entities/EntityTypeFilterBar.tsx, client/components/entities/EntityDetailHeader.tsx, client/components/entities/AliasChipList.tsx, client/components/entities/AliasChip.tsx, client/components/entities/RelatedEntityChip.tsx, client/components/entities/SplitAliasDialog.tsx, client/pages/EntitiesPage.tsx, client/pages/EntityDetailPage.tsx, client/hooks/useTauri.ts, client/components/layout/Sidebar.tsx, client/App.tsx, client/lib/mock-data.ts, client/pages/DocumentPage.tsx</files>
  <read_first>
    - client/hooks/useTauri.ts (queryKeys factory + useDocument + useToggleFavorite patterns to mirror)
    - client/lib/mock-data.ts (existing mocks; we add mock canonicals + related entities for browser-dev fallback)
    - client/pages/SpacesPage.tsx (analog for EntitiesPage — full page structure, skeleton, empty state, grid)
    - client/pages/SpaceDetailPage.tsx (analog for EntityDetailPage — breadcrumb, header, sectioned content, error fallback)
    - client/pages/TagsPage.tsx (filter pattern for EntityTypeFilterBar reference)
    - client/components/layout/Sidebar.tsx (bottomLinks insertion point)
    - client/App.tsx (Route registration inside AppShell — placement before catch-all *)
    - client/pages/DocumentPage.tsx (currently inlines entityTypeIcon + the chip render block from Plan 05; swap for the new EntityChip)
    - client/components/ui/alert-dialog.tsx (shadcn AlertDialog primitive for SplitAliasDialog)
    - .planning/phases/06-knowledge-graph-and-native-integrations/06-UI-SPEC.md §Route 1 (/entities), §Route 2 (/entities/:id), §Component Inventory, §Per-component visual spec (EntityChip, EntityTypeBadge, EntityCard, EntityDetailHeader, AliasChip, SplitAliasDialog, RelatedEntityChip), §Sidebar Modification, §Color > Entity-type color palette, §Copywriting Contract
    - .planning/phases/06-knowledge-graph-and-native-integrations/06-PATTERNS.md §EntityChip, §EntityCard, §EntityTypeFilterBar, §SplitAliasDialog, §EntitiesPage, §EntityDetailPage, §Sidebar.tsx, §App.tsx, §useTauri.ts hook additions, §Shared Pattern F (entity icon palette), §Shared Pattern G (loading/empty/error triplet), §Shared Pattern H (toasts)
  </read_first>
  <behavior>
    - Test 1: EntityChip renders a Link to /entities/{entity.canonicalId ?? encodeURIComponent(entity.value)} with aria-label "Entity: {value}, {entityType}". Icon resolved by entityTypeIcon (Building2 for organization with text-amber-400; Mail with text-cyan-400 for email; existing colors for date/amount/person/location).
    - Test 2: EntityCard wraps a Link to /entities/{id} with type-color icon tile, canonical_name, document_count caption, and an "X aliases" caption when aliases.length > 1.
    - Test 3: EntityTypeFilterBar renders 7 pills (All + 6 types); clicking a pill calls onSelect(type | "all").
    - Test 4: EntitiesPage — uses useEntities; loading shows skeleton; empty shows "No entities yet" heading + body copy from UI-SPEC §Empty state; populated renders type-grouped grid (Person section, Organization section, etc.) with EntityCards.
    - Test 5: EntitiesPage backfill-running variant — when useBackfillStore.status === "running" AND canonicals empty, heading changes to "Extracting entities…" and body shows "{processed} / {total} processed".
    - Test 6: EntityDetailPage — useEntity returns a canonical; useEntityDocuments returns documents; useRelatedEntities returns related list. All three render in the layout per UI-SPEC §Route 2 diagram.
    - Test 7: EntityDetailPage rename — clicking the pencil icon enters edit mode (input replaces the static name); pressing Enter dispatches useRenameEntityCanonical with { id, newName }; pressing Esc cancels and restores the original name. On success a sonner toast "Renamed to '{name}'" appears.
    - Test 8: AliasChip Split — clicking the scissors button opens SplitAliasDialog (mock onConfirm); confirming dispatches useSplitEntityAlias with { canonicalId, alias }. On success a toast "Split '{alias}' into a new entity" appears with a View action navigating to the new canonical id from the mutation response.
    - Test 9: SplitAliasDialog confirm button uses accent-primary (NOT destructive red) per UI-SPEC §Color > Destructive — split is recoverable.
    - Test 10: Sidebar bottomLinks contains an Entities entry between Tags and Watched Folders using the Network icon. Active route highlights when location matches /entities or /entities/:id.
    - Test 11: useTauri hooks — queryKeys factory exports entities, entitiesByType(type), entity(id), entityDocuments(id), relatedEntities(id). All 7 hooks (5 queries + 2 mutations) callable; mutations correctly invalidate adjacent query keys per the toggleFavorite pattern.
    - Test 12: DocumentPage chip rendering — replaces the inline Link cluster (created in Plan 05 Task 2) with mapped EntityChip components. Existing functionality preserved (Link to /entities/:id, aria-label, focus ring).
    - Test 13: App.tsx — /entities and /entities/:id routes are registered inside the AppShell Route group; navigating to either URL renders the corresponding page (use MemoryRouter in tests).
  </behavior>
  <action>
Build the entity component library and pages. Mirror existing analogs:

client/components/entities/EntityChip.tsx — extract the inline chip logic from client/pages/DocumentPage.tsx (Plan 05 Task 2 added it inline). Move entityTypeIcon helper here too. Props { entity: { value: string; entityType: string; canonicalId?: string } }. Use UI-SPEC §EntityChip class set (inline-flex, items-center, gap-1.5, px-2.5, py-1, rounded-full, border, border-border-secondary, bg-bg-tertiary, hover:bg-accent-subtle, focus-visible:ring-2, focus-visible:ring-accent-primary, focus-visible:ring-offset-2, focus-visible:outline-none, transition-colors). Inner: icon + span with text-sm, text-text-primary, truncate, max-w-[160px]. Wrap in a tooltip showing the full value if truncated (use shadcn Tooltip).

client/components/entities/EntityTypeBadge.tsx — pill-only badge per UI-SPEC §EntityTypeBadge. Props { entityType: string }. Layout inline-flex, items-center, gap-1, px-2, py-0.5, rounded-md. Background bg-{type}-400/10, foreground text-{type}-400, border border-{type}-400/30 — use a tokenMap helper to resolve the 6 type colors from UI-SPEC §Color > Entity-type color palette. Text content is capitalized type name ("Person", "Organization", "Location", "Date", "Amount", "Email").

client/components/entities/EntityCard.tsx — analog of SubSpaceCard in SpaceDetailPage.tsx lines 61-80. Props { entity: EntitySummary }. Wrapped Link to /entities/{id}; outer classes "card p-4 hover:shadow-md hover:border-accent-primary/50 transition-all". Inside: row with type-color icon tile, canonical_name, doc-count caption. If summary surface includes aliases.length info via a wider type (consider passing CanonicalEntity), show "X aliases" caption.

client/components/entities/EntityTypeFilterBar.tsx — 7 pill toggle group per UI-SPEC §EntityTypeFilterBar + PATTERNS.md FilterChip analog. Props { active: string; onSelect: (v: string) => void }. Render pills: all, person, organization, location, date, amount, email. Active pill uses accent-primary; inactive uses bg-bg-secondary + border-border-primary. Use existing FilterChip styling pattern from SearchPage.tsx lines 36-58 verbatim.

client/components/entities/EntityDetailHeader.tsx — per UI-SPEC §EntityDetailHeader. Props { entity: CanonicalEntity; onRename: (newName: string) => void }. Layout flex items-start gap-4. Icon tile (type icon 28 inside p-3 rounded-lg bg-{type}-400/10 text-{type}-400). Right column flex-1 space-y-2 with canonical name (page-title classes), EntityTypeBadge next to it, document-count caption. Rename pencil (Pencil 16 in text-text-tertiary) inline. Edit mode replaces name with an input styled to match (input-base text-2xl font-semibold py-1) plus Save (Check 16 in text-accent-primary) + Cancel (X 16 in text-text-tertiary) buttons. Enter submits, Esc cancels, focus auto-selects on enter to edit mode.

client/components/entities/AliasChipList.tsx — per UI-SPEC §Route 2 Aliases section. Props { aliases: string[]; canonicalName: string; onSplit: (alias: string) => void }. Renders the section heading "Aliases ({n})", description copy from UI-SPEC ("These surface forms were merged into this entity. If any look wrong, click Split to separate."), and a flex-wrap of AliasChip components. Hides itself entirely when aliases.length === 1 AND aliases[0] === canonicalName per UI-SPEC §States.

client/components/entities/AliasChip.tsx — per UI-SPEC §AliasChip. Props { alias: string; isCanonical: boolean; onSplit: () => void }. Layout "group inline-flex items-center gap-2 px-3 py-1.5 rounded-full bg-bg-tertiary border border-border-secondary". Text in text-sm text-text-primary. Scissors button (size 14) in "opacity-0 group-hover:opacity-100 group-focus-within:opacity-100 transition-opacity"; aria-label "Split alias '{alias}' off"; hidden when isCanonical=true. When isCanonical=true, prefix with a Check 12 in text-accent-primary (UI-SPEC §AliasChip Canonical indicator).

client/components/entities/RelatedEntityChip.tsx — per UI-SPEC §RelatedEntityChip. Wraps EntityChip + a count badge on the right showing "× {co_occurrence_count}" in text-[10px] text-text-tertiary tabular-nums. Tooltip "Co-occurs in {n} documents with this entity".

client/components/entities/SplitAliasDialog.tsx — per UI-SPEC §SplitAliasDialog. Wraps shadcn AlertDialog. Props { alias: string; open: boolean; onOpenChange: (v: boolean) => void; onConfirm: () => void }. Title "Split \"{alias}\" off?". Description copy from UI-SPEC (verbatim). Confirm button label "Split alias"; uses accent-primary styling (NOT destructive red per UI-SPEC §Color > Destructive). Cancel button uses shadcn default.

client/pages/EntitiesPage.tsx (default export) — per UI-SPEC §Route 1 + PATTERNS.md §EntitiesPage. Layout space-y-6. Page title "Entities". Body "Click any entity to see every document mentioning it." EntityTypeFilterBar (state-managed). useEntities() drives the grid; useEntitiesByType(filter) when filter !== "all". Grouped-by-type when filter === "all" (sections per type with grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-3 of EntityCards). Loading state via SkeletonGrid (mirror SpacesPage.tsx lines 22-38). Empty state per UI-SPEC §States with Network icon 40. Backfill-running variant reads useBackfillStore. Error state per UI-SPEC §States.

client/pages/EntityDetailPage.tsx (default export) — per UI-SPEC §Route 2 + PATTERNS.md §EntityDetailPage. Layout space-y-8. Breadcrumb "Home › Entities › {canonicalName}" (mirror SpaceDetailPage.tsx breadcrumb pattern). EntityDetailHeader. AliasChipList. Two-column 7/5 split (lg:grid lg:grid-cols-12 gap-6 with the Documents column lg:col-span-7 and Related lg:col-span-5; stacks below 900px). Documents section (mirror SpaceDetailPage.tsx Documents section lines 201-218 with copy "Documents mentioning this"). Related entities section (flex-wrap of RelatedEntityChip, ranked desc by co_occurrence_count). Empty/loading/error per UI-SPEC §States table. Inline edit handler for rename → useRenameEntityCanonical mutation. Split handler → SplitAliasDialog → useSplitEntityAlias mutation → toast with "View" action navigating to the returned new canonical id.

client/hooks/useTauri.ts — extend the queryKeys factory with:
- entities: ["entities"] as const
- entitiesByType: (type: string) => ["entities", "byType", type] as const
- entity: (id: string) => ["entities", id] as const
- entityDocuments: (id: string) => ["entities", id, "documents"] as const
- relatedEntities: (id: string) => ["entities", id, "related"] as const

Add 7 hooks mirroring the useDocument + useToggleFavorite patterns (lines 135-146 + 224-234):
- useEntities() → tauriInvoke<EntitySummary[]>("get_entities_by_type", { entityType: undefined }, fallback)
- useEntitiesByType(type) → tauriInvoke<EntitySummary[]>("get_entities_by_type", { entityType: type }, fallback); enabled: Boolean(type)
- useEntity(id) → tauriInvoke<CanonicalEntity>("get_entity", { id }, fallback); enabled: Boolean(id)
- useEntityDocuments(id) → tauriInvoke<Document[]>("get_documents_for_entity", { id }, fallback)
- useRelatedEntities(id, min?: number, limit?: number) → tauriInvoke<RelatedEntity[]>("get_related_entities", { id, minCoOccurrence: min, limit }, fallback)
- useRenameEntityCanonical() → useMutation calling tauriInvoke<CanonicalEntity>("rename_entity_canonical", { id, newName }, () => undefined); onSuccess invalidates queryKeys.entity(id) + queryKeys.entities
- useSplitEntityAlias() → useMutation calling tauriInvoke<CanonicalEntity>("split_entity_alias", { canonicalId, alias }, () => undefined); onSuccess invalidates queryKeys.entity(canonicalId) + queryKeys.entities + queryKeys.entityDocuments(canonicalId)

Fallbacks return mock data from mock-data.ts (extend it with mockEntities and mockRelatedEntities arrays — 5-10 mock entities covering all 6 types).

client/components/layout/Sidebar.tsx — add { path: "/entities", label: "Entities", icon: Network } to bottomLinks between Tags and Watched Folders entries. Import Network from lucide-react.

client/App.tsx — register two routes inside the AppShell Route group, BEFORE the catch-all "*":
- <Route path="/entities" element={<EntitiesPage />} />
- <Route path="/entities/:id" element={<EntityDetailPage />} />
Add imports for EntitiesPage and EntityDetailPage at the top.

client/pages/DocumentPage.tsx — REPLACE the inline entity-chip render block (added in Plan 05 Task 2 at the entity rendering section) with EntityChip mapping. The local entityTypeIcon function in DocumentPage.tsx is now redundant — remove it AND remove the per-icon lucide imports that became unused (Building2, Mail, Users, MapPin, Calendar, DollarSign — but only remove imports that grep confirms are unused elsewhere in the file). Tests must keep passing.

client/lib/mock-data.ts — append mockEntities and mockRelatedEntities exports. mockEntities: array of EntitySummary covering at least 2 of each type. mockRelatedEntities: keyed by canonical id, returns array of RelatedEntity. Use realistic but fictional names ("John Smith", "Acme Corp", "Brooklyn", "2024-03-15", "$1,200", "support@example.com").

Tests: create vitest test files for each major piece. Highest-priority tests:
- client/components/entities/EntityChip.test.tsx (Test 1)
- client/components/entities/EntityCard.test.tsx (Test 2)
- client/components/entities/EntityTypeFilterBar.test.tsx (Test 3)
- client/pages/EntitiesPage.test.tsx (Tests 4, 5)
- client/pages/EntityDetailPage.test.tsx (Tests 6, 7, 8)
- client/components/entities/SplitAliasDialog.test.tsx (Test 9)
- client/components/layout/Sidebar.test.tsx (Test 10, if doesn't exist as a file just create a focused unit test)
- client/hooks/useTauri.test.tsx (Test 11; extend if file exists)
- client/App.test.tsx (Test 13; new file with MemoryRouter)
Use MemoryRouter + QueryClientProvider wrappers per existing test conventions.
  </action>
  <verify>
    <automated>bun run test --run client/components/entities client/pages/EntitiesPage client/pages/EntityDetailPage client/hooks/useTauri 2>&1 | tail -80; bun run tsc --noEmit 2>&1 | head -40; for f in EntityChip EntityTypeBadge EntityCard EntityTypeFilterBar EntityDetailHeader AliasChipList AliasChip RelatedEntityChip SplitAliasDialog; do test -f client/components/entities/$f.tsx && echo OK_$f || echo FAIL_$f; done; test -f client/pages/EntitiesPage.tsx && echo OK_entities_page || echo FAIL_entities_page; test -f client/pages/EntityDetailPage.tsx && echo OK_entity_detail_page || echo FAIL_entity_detail_page; grep -q 'useEntities' client/hooks/useTauri.ts && echo OK_useEntities || echo FAIL_useEntities; grep -q 'useRenameEntityCanonical' client/hooks/useTauri.ts && echo OK_rename_hook || echo FAIL_rename_hook; grep -q 'useSplitEntityAlias' client/hooks/useTauri.ts && echo OK_split_hook || echo FAIL_split_hook; grep -q 'path: "/entities"' client/components/layout/Sidebar.tsx && echo OK_sidebar_link || echo FAIL_sidebar_link; grep -q 'path="/entities"' client/App.tsx && echo OK_route_registered || echo FAIL_route_registered; grep -q 'EntityChip' client/pages/DocumentPage.tsx && echo OK_docpage_uses_chip || echo FAIL_docpage_chip_missing</automated>
  </verify>
  <acceptance_criteria>
    - All 9 entity component files exist under client/components/entities/
    - client/pages/EntitiesPage.tsx exists with default export and contains literal substring useEntities (or useEntitiesByType)
    - client/pages/EntityDetailPage.tsx exists with default export and contains literal substrings useEntity, useEntityDocuments, useRelatedEntities
    - client/hooks/useTauri.ts queryKeys factory contains all 5 new keys (entities, entitiesByType, entity, entityDocuments, relatedEntities) and exports the 7 hooks listed above
    - client/components/layout/Sidebar.tsx bottomLinks contains entry with path "/entities" using the Network icon
    - client/App.tsx Route element for "/entities" and "/entities/:id" present inside the AppShell Route group
    - client/pages/DocumentPage.tsx renders EntityChip components (grep for `<EntityChip`); the local entityTypeIcon helper is removed (grep -c for `function entityTypeIcon` MUST be 0)
    - client/lib/mock-data.ts exports mockEntities and mockRelatedEntities
    - SplitAliasDialog confirm button uses the accent-primary class set (NOT bg-red-* / bg-destructive)
    - bun run test --run client/components/entities client/pages/EntitiesPage client/pages/EntityDetailPage client/hooks/useTauri exits 0
    - bun run tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Entity discovery UX is functional end-to-end at the unit-test level: /entities loads, /entities/:id loads, rename/split mutations dispatch, DocumentPage uses the shared EntityChip, Sidebar has the Entities link.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: BackfillIndicator + useBackfillStore + useBackfillProgress + TopBar slot + AppShell event mount + REQUIREMENTS.md update</name>
  <files>client/lib/stores.ts, client/hooks/useBackfillProgress.ts, client/components/layout/BackfillIndicator.tsx, client/components/layout/TopBar.tsx, client/components/layout/AppShell.tsx, .planning/REQUIREMENTS.md</files>
  <read_first>
    - client/lib/stores.ts (existing useIndexingStore lines 44-79 — direct analog for useBackfillStore)
    - client/components/layout/TopBar.tsx (existing indexing chip lines 37-56 — analog for BackfillIndicator placement)
    - client/components/layout/AppShell.tsx (existing event-listener mount point — useBackfillProgress() called here once)
    - client/pages/WatchedPage.tsx (existing event-listener pattern lines 45-68 for entity-backfill-progress)
    - client/lib/types.ts (EntityBackfillProgress type from Plan 04)
    - .planning/REQUIREMENTS.md (existing structure — where to add Knowledge Graph section)
    - .planning/phases/06-knowledge-graph-and-native-integrations/06-UI-SPEC.md §Cross-cutting Surface 1 (TopBar Backfill Indicator — full visual spec)
    - .planning/phases/06-knowledge-graph-and-native-integrations/06-PATTERNS.md §BackfillIndicator, §useBackfillProgress, §client/lib/stores.ts (full useBackfillStore shape)
    - .planning/phases/06-knowledge-graph-and-native-integrations/06-RESEARCH.md §Phase Requirements (the table of new requirement IDs to add to REQUIREMENTS.md)
  </read_first>
  <behavior>
    - Test 1: useBackfillStore initial state — status="idle", processed=0, total=0, error=null.
    - Test 2: useBackfillStore.setProgress({ status: "running", processed: 25, total: 100 }) updates exactly those fields.
    - Test 3: useBackfillStore.reset() returns store to initial state.
    - Test 4: useBackfillProgress mounts a tauri event listener for "entity-backfill-progress" via @tauri-apps/api/event::listen; on payload arrival, writes through setProgress. Unmount unlistens (cleanup function called).
    - Test 5: When isTauri() returns false, useBackfillProgress does NOT call listen (no Tauri assumption in browser dev).
    - Test 6: BackfillIndicator renders nothing when store.status === "idle".
    - Test 7: BackfillIndicator renders chip with Brain icon (animate-pulse) + "Extracting entities {p}/{t}" when status === "running"; tooltip shows {processed} of {total} on hover.
    - Test 8: BackfillIndicator on status === "complete" briefly shows "Done extracting entities" for 3 seconds then resets store to idle (cleanup of post-complete state — use setTimeout in a useEffect).
    - Test 9: BackfillIndicator on status === "error" shows AlertCircle + red color + error tooltip.
    - Test 10: TopBar conditionally renders BackfillIndicator between the existing indexing chip and the theme toggle. AppShell mounts useBackfillProgress() at the top of its body.
    - Test 11: REQUIREMENTS.md after edit contains a "## Knowledge Graph" section with KG-01..KG-05 entries AND lines under UX section for UX-05 and UX-06 AND a line under Frontend Pages section for PAGE-13. Traceability table at the bottom is extended with 8 new rows mapping each ID to "Phase 6 | Not started" (status will be updated post-completion).
  </behavior>
  <action>
client/lib/stores.ts — append useBackfillStore mirroring useIndexingStore (lines 44-79). Interface BackfillState { status: "idle" | "running" | "complete" | "error"; processed: number; total: number; error: string | null; setProgress: (p: Partial<BackfillState>) => void; reset: () => void; }. create<BackfillState>((set) => ({ status: "idle", processed: 0, total: 0, error: null, setProgress: (p) => set((s) => ({ ...s, ...p })), reset: () => set({ status: "idle", processed: 0, total: 0, error: null }) })).

client/hooks/useBackfillProgress.ts (NEW) — mounts the Tauri event listener once and routes payloads into the store. Use the existing pattern from WatchedPage.tsx lines 45-68 verbatim (dynamic import of @tauri-apps/api/event; isTauri guard; cleanup via return). Hook is parameter-less; intended to be mounted at the AppShell level once. Inside the listener: useBackfillStore.getState().setProgress(payload).

client/components/layout/BackfillIndicator.tsx (NEW) — UI-SPEC §Cross-cutting Surface 1 exactly. Reads useBackfillStore. Renders nothing when status === "idle". For status === "running": chip with classes "flex items-center gap-2 rounded-md bg-accent-primary/10 px-2.5 py-1.5 text-xs text-accent-primary"; Brain icon 14 (animate-pulse); label "Extracting entities {processed}/{total}" wrapped in "hidden sm:inline" for narrow widths. Wrap in shadcn Tooltip (TooltipTrigger asChild) with content showing the title line and processed-of-total sub line. For status === "complete": same chip swapped to a success variant for 3 seconds, then call store.reset() in a useEffect with timeout to hide the chip. For status === "error": swap to AlertCircle icon + text-red-400 color; tooltip shows the error message; click dismisses (calls reset()).

client/components/layout/TopBar.tsx — add a BackfillIndicator slot between the existing indexing chip (lines 37-56) and the theme toggle. No conditional wrapper — BackfillIndicator owns its own visibility. Import BackfillIndicator.

client/components/layout/AppShell.tsx — call useBackfillProgress() at the top of the component body (next to other once-per-app effects). Import the hook. This is the single mount point that subscribes to the Tauri event for the entire app lifetime.

.planning/REQUIREMENTS.md — edit to add the 8 new requirement IDs:
1. Locate the "## Frontend Pages" section. After the PAGE-12 entry, add a new line:
   `- [ ] **PAGE-13**: Document detail in-app preview for PDF, image, plain-text, and markdown files (no 200-char excerpt)`
2. Locate the "## UX" section. After UX-04, add two lines:
   `- [ ] **UX-05**: Add Watched Folder opens a native OS folder picker (no manual path typing)`
   `- [ ] **UX-06**: Open in Finder / Open with default app from Document detail AND search results`
3. Add a new "## Knowledge Graph" section before "## v2 Requirements". List:
   `- [ ] **KG-01**: Entities extracted from documents appear as graph nodes; clicking surfaces every document mentioning them`
   `- [ ] **KG-02**: Entity normalization merges aliases (e.g., "123 Main St" / "Main Street property") via embedding similarity`
   `- [ ] **KG-03**: Knowledge graph queryable via IPC — entities by type, documents for entity, related entities`
   `- [ ] **KG-04**: Rename canonical name + Split alias actions on /entities/:id`
   `- [ ] **KG-05**: NER backfill runs on startup, emits progress events, UI stays responsive`
4. Extend the Traceability table at the bottom with 8 new rows: each requirement ID mapped to "Phase 6" / "Not started". After Phase 6 completes, the orchestrator will flip status to "Complete".
5. Update the v1 requirements count at the bottom from 58 to 66.

Tests:
- client/lib/stores.test.ts — tests 1-3 for useBackfillStore.
- client/hooks/useBackfillProgress.test.tsx — tests 4-5 (mount/unmount, browser-mode no-op). Mock @tauri-apps/api/event::listen.
- client/components/layout/BackfillIndicator.test.tsx — tests 6-9.
- client/components/layout/TopBar.test.tsx — Test 10 (if file doesn't exist, create with focused render checking BackfillIndicator presence).
  </action>
  <verify>
    <automated>bun run test --run client/lib/stores client/hooks/useBackfillProgress client/components/layout/BackfillIndicator client/components/layout/TopBar 2>&1 | tail -50; bun run tsc --noEmit 2>&1 | head -30; grep -q 'useBackfillStore' client/lib/stores.ts && echo OK_store || echo FAIL_store; grep -q 'entity-backfill-progress' client/hooks/useBackfillProgress.ts && echo OK_event_name || echo FAIL_event_name; grep -q 'useBackfillProgress' client/components/layout/AppShell.tsx && echo OK_appshell_mount || echo FAIL_appshell_mount; grep -q 'BackfillIndicator' client/components/layout/TopBar.tsx && echo OK_topbar_slot || echo FAIL_topbar_slot; grep -q '## Knowledge Graph' .planning/REQUIREMENTS.md && echo OK_kg_section || echo FAIL_kg_section; for id in KG-01 KG-02 KG-03 KG-04 KG-05 PAGE-13 UX-05 UX-06; do grep -q "**$id**" .planning/REQUIREMENTS.md && echo OK_$id || echo FAIL_$id; done</automated>
  </verify>
  <acceptance_criteria>
    - client/lib/stores.ts contains literal substring `useBackfillStore` and exports it
    - client/hooks/useBackfillProgress.ts contains literal substrings `entity-backfill-progress` and `useBackfillStore.getState()`
    - client/components/layout/BackfillIndicator.tsx contains literal substrings `Brain`, `Extracting entities`, `AlertCircle`, `useBackfillStore`
    - client/components/layout/TopBar.tsx contains literal substring `<BackfillIndicator`
    - client/components/layout/AppShell.tsx contains literal substring `useBackfillProgress()`
    - .planning/REQUIREMENTS.md contains a `## Knowledge Graph` section AND all 8 requirement IDs (KG-01 through KG-05, PAGE-13, UX-05, UX-06) appear as `**ID**:` entries
    - .planning/REQUIREMENTS.md Traceability table has 8 new rows mapping each ID to Phase 6
    - bun run test --run for the touched files exits 0
    - bun run tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Backfill UI is wired end-to-end: AppShell mounts the listener once, TopBar shows the chip with live progress, REQUIREMENTS.md formally adopts the 8 new requirement IDs as v1 obligations.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: End-to-end UX checkpoint — full Phase 6 manual verification</name>
  <what-built>Phase 6 is now fully assembled across 6 plans. This checkpoint exercises every user-facing surface introduced in the phase to verify the full UX works in a real Tauri dev runtime — covering NER backfill, native folder picker, in-app preview, Open-in-OS, entity discovery, alias merge/split, and rename. The automated tests verify behavior in isolation; this checkpoint verifies they compose correctly.</what-built>
  <how-to-verify>
1. Run `bun tauri dev` and wait for the window to appear. Open the dev-tools console — confirm no CSP violations and no missing-plugin errors.

2. Backfill (KG-05): On first launch after Plan 03's lib.rs changes, the TopBar should show "Extracting entities X/Y" with a pulsing Brain icon for a few seconds (or minutes for large libraries). When it switches to "Done extracting entities" briefly then disappears, backfill is complete. If your dev DB is empty/small, the chip may flash briefly with "complete".

3. Native folder picker (UX-05): Navigate to /watched. Click Add Folder. Confirm the native OS folder picker opens (macOS Finder dialog, Windows Explorer dialog, or Linux GTK dialog). Pick a directory. Confirm it appears in the watched list. Click Add Folder again, hit Cancel — confirm nothing happens silently (no toast, no dialog popup).

4. Entity discovery (KG-01): Click the new Entities link in the sidebar (under Tags, with the Network icon). Confirm the /entities index loads grouped by type. Click an entity card.

5. Entity detail (KG-01, KG-04): On /entities/:id, confirm:
   - Header shows canonical name + type badge + document count
   - Aliases section (if more than one alias) shows the alias chips
   - Documents-mentioning section lists clickable documents
   - Related entities chips appear on the right

6. Rename canonical (KG-04): Click the pencil icon next to the canonical name. Type a new name. Press Enter. Confirm a sonner toast "Renamed to '{new name}'" appears AND the name updates AND a refresh of /entities/:id shows the new name.

7. Split alias (KG-04): On an entity with multiple aliases, hover an alias chip. Click the scissors button that appears. Confirm SplitAliasDialog opens with the alias text in the title. Click Split alias. Confirm a toast "Split '{alias}' into a new entity" appears AND the alias chip is removed from the current entity. Click the "View" action in the toast — confirm navigation to the new canonical's detail page.

8. Document preview (PAGE-13): Open a PDF document via /search or directly via /document/{id}. Confirm:
   - The 200-char excerpt block is GONE
   - In its place a PDF iframe renders with zoom/scroll/find available
   - Try an image document → confirm img renders
   - Try a .txt document → confirm text renders in a monospaced pre block
   - Try a .md document → confirm markdown renders with formatting (headings, lists, links)

9. Size guard (PAGE-13 D-15): If you have a PDF larger than 50 MB, opening it should show the SizeGuardCard with "Large file (X MB)" + "Load preview" and "Open in default app" buttons. Click Load preview — confirm the iframe loads (may take a moment).

10. Open in OS (UX-06): On any document detail page, click "Open in default app" — confirm the system default app launches the file. Click "Reveal in Finder" (or "Show in file manager" on Linux/Windows) — confirm Finder opens with the file selected.

11. Context menu (UX-06 D-18): Navigate to /search, /recent, /favorites, /spaces/:id. Right-click any document row. Confirm a context menu appears with Open / Open in default app / Reveal in Finder. Click Open in default app — confirm OS handler launches.

12. Sidebar nav (D-10): Confirm the Entities link in the sidebar highlights when on /entities or /entities/:id (active link styling matches existing pattern).

13. Clickable entity chips on DocumentPage (D-09): On /document/{id}, scroll to the entity chips in the right metadata sidebar. Confirm each chip is now a clickable Link with focus ring + hover state. Click one — confirm navigation to /entities/{canonicalId}.

14. Markdown XSS posture (PAGE-13 T-06-MD-XSS): If you have any markdown file containing inline HTML (e.g. `<script>` or `<img onerror=...>`), open it. Confirm the script/HTML is NOT executed — it should render as escaped text. Inspect the DOM via dev tools to confirm no real script element exists.

15. Performance: throughout testing, confirm the UI stays responsive — no multi-second freezes during backfill, no jank when navigating between entity pages.

If ANY of these 15 verifications fail, capture the failure (screenshot + console log) in the resume signal. The orchestrator will route the failure to gap closure.
  </how-to-verify>
  <resume-signal>Type "approved — all 15 checks pass" if every step worked. Otherwise, list which checks failed and describe the failure (e.g., "step 5: related entities section was empty even though the entity has 12 docs; expected at least 1 related"). The orchestrator will use the failures to plan a gap-closure phase.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tauri event bus → Zustand store | entity-backfill-progress payload is trusted (server-emitted); useBackfillStore.setProgress writes payload fields directly |
| Frontend rename/split inputs → IPC | new_name and alias strings flow from user input to rename_entity_canonical / split_entity_alias commands |
| Mutation onSuccess → query invalidation | React Query refetches use trusted query keys |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-RENAME-INPUT | Tampering | User-typed new canonical name could be arbitrarily long or contain control chars | mitigate | Plan 03 IPC accepts String; trim + length-cap at the EntityStore.rename_canonical level (max 200 chars enforced server-side, Plan 03 acceptance). UI clamps input maxLength=200 attribute. |
| T-06-EVENT-SPOOF | Spoofing | Renderer could fire fake entity-backfill-progress events via window.__TAURI__ internals | accept | Tauri 2's IPC event bus is browser-process-only; events emitted from the main process flow one-way. Same trust model as existing index-progress event. Documented. |
| T-06-MUTATION-RACE | Tampering | Two rapid renames could race on the EntityStore mutex | mitigate | Plan 03 EntityStore uses std::sync::Mutex; serializes writes. React Query mutationFn dispatch is sequential; useMutation already debounces double-clicks via isLoading state. |
| T-06-FAILED-MUTATION | Repudiation | A failed split silently leaves UI inconsistent if not handled | mitigate | useSplitEntityAlias onError shows toast.error per UI-SPEC §Error state copy; onSuccess invalidates queries to force refetch. Test 8 partial — extend with onError test in Plan 06 task 1 tests. |
| T-06-CHECKPOINT-BYPASS | Tampering | Auto-yolo mode could skip the end-to-end checkpoint | mitigate | Task 3 declared as gate="blocking" (NOT blocking-human; standard blocking gate that respects auto_advance). User in yolo mode still gets the checkpoint summary but may auto-advance; project config has workflow.verifier=true so this is an additional safety net. |
| T-06-MD-XSS-REGRESSION | Tampering / Injection | After Plan 05 extracted entity chips, Plan 06 may inadvertently re-introduce rehype-raw via the EntityChip extraction | mitigate | EntityChip.tsx imports do not touch react-markdown at all; the regression test from Plan 05 (MarkdownPreview.test.tsx) still runs in CI. |
</threat_model>

<verification>
- bun run test --run exits 0 across all new test files
- bun run tsc --noEmit exits 0
- Task 3 manual checkpoint passes for all 15 verifications
- REQUIREMENTS.md formal-status: 8 new requirement IDs declared as v1 obligations
</verification>

<success_criteria>
- KG-01 (entities-as-graph-nodes with click-through to docs) — /entities → /entities/:id → Documents-mentioning + Related entities all working
- KG-03 (queryable via IPC) — 7 React Query hooks exercising all 6 entity commands
- KG-04 (Rename + Split UX) — inline rename + SplitAliasDialog + toast feedback
- KG-05 (backfill progress UI) — BackfillIndicator + useBackfillProgress + useBackfillStore live-update from Tauri events
- D-09 (clickable chips), D-10 (sidebar Entities), D-18 (context menu finalized via Plan 04 + DocumentPage header via Plan 05)
- REQUIREMENTS.md formally adopts KG-01..KG-05, UX-05, PAGE-13, UX-06
- End-to-end UX verified in real Tauri runtime
</success_criteria>

<output>
Create .planning/phases/06-knowledge-graph-and-native-integrations/06-06-SUMMARY.md when done. Include:
- Confirmation that Task 3 checkpoint passed all 15 verifications (or list which failed)
- Estimated backfill runtime on the dev box
- Whether mock-data.ts additions caused any TS regression
- Any visual regressions observed when extracting EntityChip from DocumentPage's inline render
- Note that REQUIREMENTS.md now has 66 v1 requirement IDs (was 58)
</output>
